% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ZINBSAR.R
\name{estimate_parameters}
\alias{estimate_parameters}
\title{Quasi-Likelihood Estimation for a Spatial Negative Binomial Model}
\usage{
estimate_parameters(
  y,
  z,
  x,
  W,
  gamma,
  beta,
  rho1,
  rho2,
  t,
  max_iter = 100,
  tol = 1e-06
)
}
\arguments{
\item{y}{A numeric vector of observed counts.}

\item{z}{A matrix of covariates for `p_i`.}

\item{x}{A matrix of covariates for `lambda_i`.}

\item{W}{A spatial weight matrix (n x n).}

\item{gamma}{Initial coefficient vector for `p_i`.}

\item{beta}{Initial coefficient vector for `lambda_i`.}

\item{rho1}{Initial spatial parameter for `p_i`.}

\item{rho2}{Initial spatial parameter for `lambda_i`.}

\item{t}{Initial dispersion parameter of the negative binomial distribution.}

\item{max_iter}{Maximum number of iterations for convergence.}

\item{tol}{Convergence tolerance.}
}
\value{
A list with estimated parameters `gamma`, `beta`, `rho1`, `rho2`, and `t`.
}
\description{
Iterative estimation of gamma, beta, rho1, and rho2 in a spatial negative binomial model.
}
\examples{
set.seed(123)
n <- 100
y <- rpois(n, lambda = 5)  
z <- matrix(runif(n * 3), n, 3)
x <- matrix(runif(n * 3), n, 3)
W <- matrix(runif(n * n), n, n)
diag(W) <- 0
W <- W/rowSums(W)
gamma <- rep(0, 3)
beta <- rep(0, 3)
rho1 <- 0.1
rho2 <- 0.1
t <- 1
result <- estimate_parameters(y, z, x, W, gamma, beta, rho1, rho2, t)
print(result)

}
